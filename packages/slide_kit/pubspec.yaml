name: slide_kit
description: 登壇資料の共通部品（テーマ / 歩くフッター / 自己紹介スライド / App スキャフォールド）。
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_deck: ^0.28.1
  flutter_screenutil: ^5.9.3
  gap: ^3.0.1
  tutorial_coach_mark: ^1.3.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
